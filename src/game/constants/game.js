/**
 * Set up you main game constant values here
 */

export const FPS = 60;
export const FRAME_TIME = 1000 / FPS; //16.66

export const SCREEN_WIDTH = 256;
export const SCREEN_HEIGHT = 254;

// this is the size of the tile based on the stage picture
export const TILE_SIZE = 16;

// Camera slightly offset
export const HALF_TILE_SIZE = TILE_SIZE / 2;

// Height of hud panel for later
export const STAGE_OFFSET_Y = 24;

export const NO_PLAYERS = 2;
export const MAX_WINS = 3;
// constatnt value for debug, can be set true and false(on and off)
export const DEBUG = true;
